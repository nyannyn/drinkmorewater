# 更新紀錄

## Unreleased

### 跨裝置同步（桌面 ↔ 手機）
- **裝置配對碼連動**：一台產生 6 位配對碼、另一台輸入即可共享飲水紀錄，無需 email / 密碼。桌面、手機設定頁皆可操作。
- **正確的多裝置合併**：每台裝置只回報自身每日貢獻，伺服器依日期加總；多裝置就算各自離線記錄，同步後**不重複計數也不掉資料**。設定（間隔 / 目標 / 語言…）以時間戳 last-write-wins 合併。
- **共用 core**（`shared/`）：schema / 跨日重設 / 記錄 / 週統計 / 合併演算法抽成跨平台共用純函式，桌面與手機共用同一份真相來源，去除重複實作。
- **自架同步後端**（`server/`）：Node 22 內建 `node:sqlite`，零 npm 依賴，附 Dockerfile / docker-compose，可自行部署到 VPS / Fly.io / Railway。
- **離線優先**：未連動或斷線時行為與原本完全相同；同步失敗不影響使用。
- **測試**：新增 shared 純函式測試與 server 端到端測試（起真伺服器 + 真 client 跑兩裝置全流程），並納入 CI。

## 1.6.2

### 設定頁 UI 改版
- **Soft UI 風格全面翻新**：溫暖背景色、浮動陰影帶主題色調、充裕留白，移除生硬邊框。
- **水滴標題合併進度卡片**：標題區與進度環整合為一體，視覺更聚焦。
- **關於 / 隱私區塊移至統計頁**：搭配 SVG 圖示，改為輕量分隔線式排版。
- **新增隱私權說明、回報問題 / 許願入口**：統計頁底部一鍵前往 GitHub Issue。

### 下載頁
- **專業級 Dark Mode 下載頁**：Glassmorphism 風格、中英雙語介紹。
- **各平台直連下載**：動態抓取最新 Release，加 loading 狀態與硬編碼 fallback。
- **防呆機制**：偵測到不同 OS 時彈出確認提示、加入歷史版本入口。

## 1.5.0

### Bug 修復
- **音效不再「用久了失效」**：喝滿水的「叮」聲改用共用 AudioContext，修正每次新建導致數次後撞上瀏覽器 context 上限的資源洩漏。
- **多語涵蓋補齊**：系統通知、托盤選單 / tooltip、懸浮水杯提示文字現在會跟隨所選語言（繁中 / 簡中 / English / 日本語），不再固定中文；切換語言時托盤即時更新。
- **設定開關不再閃爍**：首屏在資料載入完成後才顯示，消除主題與開關狀態的瞬間跳動。
- **數值輸入更穩健**：每日目標 / 每次飲水量超出範圍時自動修正回合法值並回寫，不再靜默失敗。
- **多螢幕**：提醒水杯改在游標所在的螢幕彈出，不再固定主螢幕。
- **「過去 7 天」**：圖表對齊為最近 7 天，與標題一致。

### UI
- **本地打包字型**：字型內嵌打包，離線可用、無字型跳動（FOUT），不再依賴 Google Fonts 線上載入。
- **達標慶祝**：達到每日目標時，進度條閃動並顯示達標徽章。
- **微互動**：卡片與設定項進場淡入；尊重系統「減少動態效果」偏好。
- **可及性**：開關支援鍵盤（Tab + Space / Enter）與 `role="switch"` / `aria-checked`，並加入聚焦環。
- **主題化確認框**：重置紀錄改用符合主題的對話框，取代原生 `confirm()`。
