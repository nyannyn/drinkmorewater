# 基準線（iOS 上架準備計畫 Step 0，2026-09-15，main = 2496a68）

改任何 code 之前記錄，供 `plans/ios-store-prep.md` 的驗收條件比對。

| 項目 | 值 | 來源 |
|---|---|---|
| `ios.yml` 截圖 artifact 張數 | 2（`01-home.png`、`02-settings.png`） | run 34765788575 |
| 截圖尺寸 | 1206 × 2622（iPhone 6.3 吋，非 App Store 6.9 吋 1320 × 2868） | 同上，PIL 實量 |
| Home 週圖長條數（新安裝） | 1 | 同上截圖目視 |
| `shared` 測試 | 20 pass / 0 fail | `node --test shared/__tests__/*.test.js` |
| `mobile` 測試 | 10 pass / 0 fail | `cd mobile && npm test` |
| `getWeeklyStats` 回傳筆數（空 log） | 1（只有今日） | `shared/tracking.js:48` |
